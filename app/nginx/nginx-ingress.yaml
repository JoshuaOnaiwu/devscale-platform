apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devscale-nginx-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: devscale-nginx-service
            port:
              number: 80